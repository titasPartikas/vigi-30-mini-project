import "./header.scss";
import Input from "../Input/Input";

const Header = () => {
  return (
    <header className="header">
      <p className="logo">Easy Foods</p>
      <Input />
    </header>
  );
};

export default Header;
